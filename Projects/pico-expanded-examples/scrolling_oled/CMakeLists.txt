add_executable(scrolling_oled
        scrolling_oled.c
        )

# pull in common dependencies and additional i2c hardware support
target_link_libraries(scrolling_oled pico_stdlib hardware_i2c)

# create map/bin/hex file etc.
pico_add_extra_outputs(scrolling_oled)
